{
  "name": "strangway-js-components",
  "version": "0.0.1",
  "description": "JavaScript and Foundation components for Strangway.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "vendor": "node index.js",
    "start": "node-sass --output-style compressed --source-map true -w -r ./scss/ --output ../assets/css",
    "sass": "node-sass --output-style compressed --source-map true -r ./scss/ --output ../assets/css",
    "sass:full": "node-sass --output-style expanded --source-map true -r ./scss/ --output ../assets/css"
  },
  "author": "Adam Sommer",
  "license": "MIT",
  "dependencies": {
    "foundation-sites": "^6.5.1",
    "jquery": "^3.3.1",
    "markdown": "^0.5.0",
    "node-sass": "^4.11.0",
    "vue": "^2.5.21",
    "vue-markdown": "^2.2.4",
    "vue-simple-markdown": "^1.0.8"
  }
}
